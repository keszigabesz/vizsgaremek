version: "3"
services:
    app:
        container_name: viszgaremek_Bajo_Gabor
        build:
            dockerfile: Dockerfile
            context: .
        volumes:
          - .:/app
        ports:
          - 3000:3000
            